{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --workspaces --include-workspace-root && npm --workspace @iluminaxingu/api run build && node scripts/write-build-info.mjs"
  },
  "deploy": {
    "startCommand": "npm --workspace @iluminaxingu/api run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
